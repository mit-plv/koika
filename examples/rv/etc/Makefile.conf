# Included by the Makefile generated by Koika
DEFAULT_TARGET := pass
DEFAULT_TEST := ../../tests/_build/integ/primes

CXXFLAGS := --std=c++17
CUTTLESIM_DRIVER := rvcore.cuttlesim.cpp
CUTTLESIM_ARGS := $(DEFAULT_TEST).rv32
CUTTLESIM_VCD_SCOPES := TOP top rv_core

VERILATOR_FLAGS := +define+BRAM_RUNTIME_INIT -CFLAGS -DVL_USER_FINISH
VERILATOR_DRIVER := rvcore.verilator.cpp
VERILATOR_ARGS := +VMH=$(DEFAULT_TEST).vmh
VERILATOR_WARNINGS := -Wno-fatal
VERILATOR_TOPLEVEL := top.v

# TinyFPGA config
FPGA_TOPLEVEL := top.v
FPGA_DEVICE := lp8k
FPGA_ARACHNE_PACKAGE := cm81
FPGA_ARACHNE_PCF := tinyfpga-bx.pins.pcf

.PHONY: pass
pass:
	@:
